apiVersion: v1
kind: Service
metadata:
  name: grafana-service
  labels:
    com.job.service: grafana
spec:
  type: NodePort
  ports:
    - port: 3000
      name: web
      targetPort: 3000
      nodePort: 30300
  selector:
    com.job.service: grafana