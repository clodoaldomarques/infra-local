apiVersion: v1
kind: Service
metadata:
  name: prometheus
  labels:
    com.job.service: prometheus
spec:
  type: NodePort
  selector:
    app: prometheus
  ports:
  - port: 9090
    targetPort: 9090
    nodePort: 30010