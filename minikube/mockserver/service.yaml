apiVersion: v1
kind: Service
metadata:
  name: mockserver
  labels:
    com.job.service: mockserver
spec:
  type: NodePort
  ports:
    - port: 1080
      targetPort: 1080
      nodePort: 30008
      name: http
  selector:
    com.job.service: mockserver