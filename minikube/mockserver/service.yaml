apiVersion: v1
kind: Service
metadata:
  name: mockserver-service
  labels:
    com.job.service: mockserver-service
spec:
  type: NodePort
  ports:
    - port: 1080
      targetPort: 1080
      nodePort: 30080
      name: http
  selector:
    com.job.service: mockserver